# Copy to .env and fill in. Only needed for the judge/classify/scoring stages.
OPENROUTER_API_KEY=
# Optional: override the default judge model (any OpenRouter model id)
JUDGE_MODEL=google/gemini-2.5-flash-lite
